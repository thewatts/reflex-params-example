<%= form_with(id: "new-post", model: post, local: true) do |form| %>
  <%= hidden_field_tag "post[test][]", "a" %>
  <%= hidden_field_tag "post[test][]", "a" %>
  <%= hidden_field_tag "post[test][]", "b" %>

  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit "Submit To Controller" %>
    <%= link_to "Submit to Reflex", "#", data: { reflex: "click->Post#create" } %>
  </div>
<% end %>
